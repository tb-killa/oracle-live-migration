name: Build Oracle Migration Live ISO

on:
  push:
    branches: [ "main" ]
  schedule:
    - cron: "0 2 * * *"  # täglich um 2 Uhr nachts
  workflow_dispatch:

env:
  ISO_URL: "https://yum.oracle.com/ISOS/OracleLinux/OL9/u3/x86_64/OracleLinux-R9-U3-x86_64-dvd.iso"
  ISO_NAME: "OracleLinux-R9-U3-x86_64-dvd.iso"
  ISO_DIR: "migration-live"
  BUILD_SCRIPT: "build_migration_iso.sh"
  CACHE_KEY: "oraclelinux9u3-dvd-cache"

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: oraclelinux:9

    steps:
      # 1️⃣ Repository Checkout
      - name: 🧩 Checkout Repository
        uses: actions/checkout@v4

      # 2️⃣ Strukturprüfung und Rechte
      - name: 🔍 Repository Struktur prüfen
        run: |
          echo "=== Repository Struktur prüfen ==="
          REQUIRED_FILES=(
            "$BUILD_SCRIPT"
            "overlay/usr/local/bin/migration-gui.sh"
            "overlay/root/install-tools.sh"
            "overlay/etc/systemd/system/migration-autostart.service"
          )
          for f in "${REQUIRED_FILES[@]}"; do
            if [ ! -f "$f" ]; then
              echo "❌ Fehlende Datei: $f"; exit 1
            else
              echo "✅ Gefunden: $f"
            fi
          done
          # CRLF prüfen
          if grep -rIUls $'\r' overlay/ >/dev/null 2>&1; then
            echo "⚠️ CRLF-Zeilenenden gefunden!"; exit 1
          else
            echo "✅ Keine CRLF-Zeilenenden."
          fi
          # Ausführbar setzen
          chmod +x "$BUILD_SCRIPT" overlay/usr/local/bin/migration-gui.sh || true
          echo "✅ Rechte korrekt gesetzt."

      # 3️⃣ Caches aktivieren
      - name: 🗃️ Lade ISO Cache
        id: cache_iso
        uses: actions/cache@v4
        with:
          path: ${{ env.ISO_DIR }}/${{ env.ISO_NAME }}
          key: ${{ env.CACHE_KEY }}

      - name: 🗃️ DNF Cache
        uses: actions/cache@v4
        with:
          path: /var/cache/dnf
          key: dnf-cache-${{ runner.os }}-${{ hashFiles('**/build_migration_iso.sh') }}
          restore-keys: |
            dnf-cache-${{ runner.os }}-

      # 4️⃣ Abhängigkeiten installieren
      - name: ⚙️ Installiere Build-Abhängigkeiten
        run: |
          dnf -y install oracle-epel-release-el9
          dnf -y install git wget rsync xorriso zip unzip p7zip p7zip-plugins jq diffutils coreutils util-linux tar

      # 5️⃣ ISO Download & Cache
      - name: 📦 Lade oder prüfe Oracle Linux ISO
        run: |
          set -e
          mkdir -p $ISO_DIR
          ISO_PATH="$ISO_DIR/$ISO_NAME"
          if [ -f "$ISO_PATH" ]; then
            echo "✅ ISO bereits im Cache vorhanden: $ISO_PATH"
          else
            echo "📦 Lade ISO von $ISO_URL ..."
            wget --progress=dot:giga -O "$ISO_PATH" "$ISO_URL"
            echo "✅ Download abgeschlossen."
          fi
          echo "🔍 Prüfe Datei:"
          ls -lh "$ISO_PATH"
          echo "🔢 Prüfsumme (SHA256):"
          sha256sum "$ISO_PATH" | tee iso_checksum.txt
          FILESIZE=$(stat -c%s "$ISO_PATH")
          if [ "$FILESIZE" -lt 1000000000 ]; then
            echo "❌ ISO-Datei zu klein (<1GB)!"; exit 1
          fi
          echo "✅ ISO gültig."

      # 6️⃣ Build ausführen
      - name: 🚀 Starte ISO-Build
        run: |
          echo "🚀 Starte ISO-Build..."
          bash "$BUILD_SCRIPT"

      # 7️⃣ ISO Strukturprüfung
      - name: 🧪 ISO Strukturtest
        run: |
          echo "🔍 Teste ISO-Lesbarkeit..."
          ISO_FILE=$(ls migration-live/*.iso | head -n1)
          ls -lh "$ISO_FILE"
          echo "🔍 Prüfe Bootstruktur:"
          xorriso -indev "$ISO_FILE" -report_el_torito as_mkisofs | grep -E "Boot record|BIOS|EFI" || true

      # 8️⃣ Changelog generieren
      - name: 📝 Erzeuge Changelog
        run: |
          echo "🧾 Erzeuge Changelog..."
          mkdir -p changelog
          {
            echo "## Build vom $(date -u '+%Y-%m-%d %H:%M UTC')"
            echo
            echo "### Git Änderungen:"
            git log -1 --pretty=format:"- %h %s (%an)"
            echo
            echo "### ISO Prüfsumme:"
            cat iso_checksum.txt
            echo
            echo "### Paketliste (Top 40):"
            rpm -qa --qf "%{NAME}-%{VERSION}\n" | sort | head -n 40
          } > changelog/latest.md
          cat changelog/latest.md

      # 9️⃣ Release hochladen
      - name: 📤 GitHub Release Upload
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          tag_name: nightly-${{ github.run_number }}
          name: "Oracle Migration ISO Nightly Build #${{ github.run_number }}"
          body_path: changelog/latest.md
          files: |
            migration-live/*.iso
            iso_checksum.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
